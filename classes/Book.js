class Book {
	constructor(title, author, genre, pages, rating, read) {
		this.title = title;
		this.author = author;
		this.genre = genre;
		this.pages = pages;
		this.rating = rating;
		this.read = read;
	}
}

export default Book;
