class Book {
	constructor(bookId, title, author, genre, pages, rating, read) {
		this.bookId = bookId;
		this.title = title;
		this.author = author;
		this.genre = genre;
		this.pages = pages;
		this.rating = rating;
		this.read = read;
	}
}

export default Book;
